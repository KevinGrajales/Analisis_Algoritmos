# Curso Análisis de Algoritmos

Repositorio destinado al almacenamiento de ejercicios, laboratorios y material desarrollado durante el curso de Análisis de Algoritmos.

## Estructura del repositorio

El repositorio está organizado en las siguientes carpetas:

- `laboratorios/`: contiene los cinco informes de laboratorio evaluativos desarrollados durante el semestre.
- `ejercicios-clase/`: contiene ejercicios y códigos desarrollados durante las sesiones prácticas no evaluativas.
- `benchmarks/`: contiene scripts para la medición de tiempos y generación de gráficas utilizadas en los laboratorios.

## Comandos básicos de Git

Para revisar el estado actual del repositorio se utiliza:

```bash
git status
```

Para consultar el historial de commits:

```bash
git log --oneline
```

## Flujo de trabajo

Durante el desarrollo de los ejercicios y laboratorios se utilizará el siguiente flujo básico de trabajo con Git:

1. Crear o modificar los archivos del proyecto.
2. Revisar los cambios utilizando `git status`.
3. Agregar los cambios al área de preparación con `git add`.
4. Crear un commit con un mensaje descriptivo mediante `git commit`.
5. Sincronizar los cambios con GitHub utilizando `git push`.